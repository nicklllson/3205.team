services:
  server:
    build: ./3205-server
    ports:
      - '3000:3000'
    environment:
      PORT: 3000
    restart: unless-stopped

  client:
    build: ./3205-client
    ports:
      - '5173:5173'
    volumes:
      - ./3205-client:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - server
